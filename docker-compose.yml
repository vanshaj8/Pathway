services:
  postgres:
    image: postgres:16-alpine
    container_name: pathway-postgres
    restart: unless-stopped
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-pathway}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-pathway}
      POSTGRES_DB: ${POSTGRES_DB:-pathway}
    volumes:
      - pathway_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "pg_isready -U ${POSTGRES_USER:-pathway} -d ${POSTGRES_DB:-pathway}",
        ]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  pathway_postgres_data:
